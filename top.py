# Top node: causal diagram sign, handled by bus.py
# bus.py already handles all the up/down message passing for every node.
# top.py just needs to exist as a marker that it's running — it could simply print something
# or do nothing, since bus.py does the actual work.
